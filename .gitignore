# --- Visual Studio ---
.vs/
out/
*.sln
*.suo
*.user
*.pdb
*.iobj
*.ipdb

# --- CMake & Build Output ---
cmake-build-debug/
cmake-build-release/
build/
bin/
x64/

# --- Compiled Executables & Libraries ---
*.exe
*.dll
*.lib
*.a
*.o

# --- macOS Junk (Since you work on both) ---
.DS_Store

# --- User specific data ---
# (If your shaders are in 'data', keep 'data', but ignore generated copies)
# Assuming your source data is safe, but build copies should be ignored: